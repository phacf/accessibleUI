name: CI/CD

on:
  push:
    branches:
      - main

jobs:
  start:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '14'

      - name: Install Dependencies
        run: npm install

  build:
    runs-on: ubuntu-latest
    needs: start

    steps:
      - name: Debug - Print Working Directory
        run: pwd

      - name: Debug - List Directory Content
        run: ls

      - name: Styleguidist build
        run: npm --prefix accessibleUI run styleguide:build

      - name: Build Production Webpack Code
        run: npm --prefix accessibleUI run build

  test:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Run Tests
        run: npm --prefix accessibleUI run test

  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./styleguide
